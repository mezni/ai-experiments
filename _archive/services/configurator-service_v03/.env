# Server
HOST=0.0.0.0
PORT=8080
LOG_LEVEL=debug

# Database (either use DATABASE_URL or individual vars)
#DATABASE_URL=postgres://postgres:password@localhost:5432/configurator

# Optional individual config (if DATABASE_URL not used)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=ev_db
